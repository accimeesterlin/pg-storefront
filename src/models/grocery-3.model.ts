export interface OfferCard {
  title: string;
  mainImageUrl: string;
  buttonText: string;
  discountOffer: string;
}

export interface MainCarouselItem {
  title?: string;
  mainImageUrl?: string;
  buttonLink?: string;
  buttonText?: string;
  description?: string;
}
